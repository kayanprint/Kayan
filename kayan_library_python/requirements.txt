# متطلبات نظام إدارة مكتبة ومطبعة كيان
# Kayan Library & Printing Management System Requirements

# قاعدة البيانات
# Database
sqlite3  # مدمج مع Python

# واجهة المستخدم الرسومية
# GUI Framework
tkinter  # مدمج مع Python

# معالجة التواريخ والأوقات
# Date and Time Processing
datetime  # مدمج مع Python

# معالجة JSON
# JSON Processing
json  # مدمج مع Python

# عمليات نظام التشغيل
# Operating System Operations
os  # مدمج مع Python
shutil  # مدمج مع Python

# التقارير والطباعة
# Reports and Printing
reportlab>=3.6.0  # لإنشاء ملفات PDF
matplotlib>=3.5.0  # للرسوم البيانية
pillow>=9.0.0  # لمعالجة الصور

# معالجة البيانات
# Data Processing
pandas>=1.4.0  # لمعالجة البيانات
openpyxl>=3.0.0  # لملفات Excel

# واجهة المستخدم المحسنة
# Enhanced GUI
ttkthemes>=3.2.0  # ثيمات محسنة لـ tkinter

# الطباعة
# Printing
win32print  # للطباعة على Windows (اختياري)

# النسخ الاحتياطي والضغط
# Backup and Compression
zipfile  # مدمج مع Python

# التشفير (للبيانات الحساسة)
# Encryption (for sensitive data)
cryptography>=3.4.0  # للتشفير

# التحقق من صحة البيانات
# Data Validation
validators>=0.18.0  # للتحقق من البريد الإلكتروني والهواتف

# الشبكة والإنترنت
# Network and Internet
requests>=2.27.0  # لطلبات HTTP
urllib3>=1.26.0  # لعمليات URL

# السجلات
# Logging
logging  # مدمج مع Python

# الخيوط المتعددة
# Multi-threading
threading  # مدمج مع Python

# ملاحظات التثبيت:
# Installation Notes:

# للتثبيت على Windows:
# For Windows installation:
# pip install -r requirements.txt

# للتثبيت على Linux/Mac:
# For Linux/Mac installation:
# pip3 install -r requirements.txt

# للتثبيت في بيئة افتراضية (مُنصح به):
# For virtual environment installation (recommended):
# python -m venv kayan_env
# source kayan_env/bin/activate  # Linux/Mac
# kayan_env\Scripts\activate     # Windows
# pip install -r requirements.txt

# متطلبات النظام:
# System Requirements:
# - Python 3.8 أو أحدث
# - نظام التشغيل: Windows 10/11, Linux, macOS
# - ذاكرة: 4 جيجابايت RAM كحد أدنى
# - مساحة القرص: 500 ميجابايت للتطبيق + مساحة للبيانات
# - دقة الشاشة: 1024x768 كحد أدنى (1920x1080 مُنصح به)

# الحزم الاختيارية للميزات المتقدمة:
# Optional packages for advanced features:

# لدعم قواعد البيانات الأخرى
# For other database support
# pymongo>=4.0.0  # لـ MongoDB
# psycopg2>=2.9.0  # لـ PostgreSQL
# mysql-connector-python>=8.0.0  # لـ MySQL

# للواجهات المتقدمة
# For advanced interfaces
# PyQt5>=5.15.0  # واجهة PyQt5
# tkinter-tooltip>=2.0.0  # نصائح محسنة

# للتكامل مع الخدمات السحابية
# For cloud services integration
# boto3>=1.20.0  # لـ AWS
# google-cloud-storage>=2.0.0  # لـ Google Cloud

# للأمان المتقدم
# For advanced security
# bcrypt>=3.2.0  # لتشفير كلمات المرور
# jwt>=1.3.0  # لـ JSON Web Tokens

# ملاحظة: بعض الحزم قد تتطلب مكتبات نظام إضافية
# Note: Some packages may require additional system libraries

